[package]
name = "tests"
version.workspace = true
edition.workspace = true

[dependencies]
typed-jni = { workspace = true, features = ["default", "print-throwable"] }

jni = { version = "0.21", features = ["invocation"] }
tempdir = "0.3"
